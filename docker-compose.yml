version: '3.7'

services:
  web:
    build:
      context: .
      dockerfile: ./docker/dev/Dockerfile
    ports:
      - 8080:8080
    volumes:
      - .:/app
    stdin_open: true
    tty: true
   
volumes:
  public:
    driver: local
